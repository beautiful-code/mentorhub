.add_track
  =form_for @track, :html => {:multipart => true}  do |f|
    .form-group
      =f.text_field :name, class: "", placeholder: "TRACK NAME"
    .form-group
      =f.text_field :desc, class: "", placeholder: "TRACK DESC"
    -#.form-group
      -#=f.select :track_type, options_for_types,{},class: "form-control"
    .form-group
      =f.file_field :image
    .form-group.btn_right
      =f.submit "Add Track"
